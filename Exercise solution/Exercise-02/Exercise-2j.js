// Let's say we want to always round a number down (2.8 => 2). Using Google or an A.I. tool, search for the code to do this.
decimalNumber = 2.8;
roundedNumber = Math.floor(decimalNumber);
console.log(roundedNumber);